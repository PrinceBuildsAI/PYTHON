#Q.4 Replace the double space from problem 3 with single spaces

name = "Prince is a good  boy " 

print(name.replace("  "," ")) #Replace string
print(name #strings are immutable, on output every time a new string is created by the the terminal
      )